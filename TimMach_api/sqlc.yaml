version: "2"

sql:
  - engine: "postgresql"
    schema: "db/migrations"
    queries: "db/queries"
    gen:
      go:
        package: "db"
        out: "db/sqlc"

        # 👉 Quan trọng: dùng pgx/v5 nhưng ta override hết để tránh pgtype.*
        sql_package: "pgx/v5"

        emit_json_tags: true
        json_tags_case_style: "snake"
        emit_pointers_for_null_types: true
